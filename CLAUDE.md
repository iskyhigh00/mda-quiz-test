# CLAUDE.md — mda-quiz-test

## Flujo de trabajo

- **Los cambios van al branch `test` primero**. Nunca directamente a `main`.
- Hacer commit y push a `test` al terminar cada tarea.
- El merge a `main` (producción) lo decide el usuario manualmente.
